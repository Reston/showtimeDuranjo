$(document).ready(function() {
	/*$(".endless_page_template > ul > li > a > img").click(function(e){
	// Prevent a page reload when a link is pressed
		e.preventDefault();
		toChangeImage($(this).attr("src"));
	});
	function toChangeImage (fuente) {
		//$(".imagenGrande img").attr('src',fuente).hide().fadeIn("fast");
		console.log(fuente+" LA FUENTE DE LA IMAGEN");
		var grande = $("#imagenGrande img[src*='"+fuente+"']");
		grande.addClass('flex-active-slide');
		//grande.remove();
		//$("#imagenGrande .flexslider .slides ul").append("<li><img src="+fuente+" alt='imagen seleccionada'></li>").addClass('flex-active-slide');
	} */
});